const config = {
  url: 'http://localhost:8080', // Base URL for your API
  headers: {
      'Content-Type': 'application/json', // Default content type
      // Add other default headers if needed
  },
  // Add other configuration settings here if needed
};

export default config;